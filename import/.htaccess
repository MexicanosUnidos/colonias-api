# Los scripts de esta carpeta son CLI-only y los datos son crudos/sensibles.
# Nadie necesita pedirlos por navegador — solo se ejecutan/incluyen desde PHP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
