# config/env.php tiene credenciales reales. Nadie necesita pedir esta
# carpeta por navegador — solo se incluye internamente desde PHP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
